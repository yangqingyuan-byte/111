硕士毕业论文答辩 PPT 简版指南

论文题目
《融合大语言模型语义先验与时频建模的多元长程时间序列预测方法研究》

建议页数与时长
11 页左右，控制在 10 分钟。
如果时间更紧，可压缩为 10 页：将“实验设置”和“主结果”合并。

答辩主线
不要把答辩讲成“第三章做了什么、第四章做了什么、第五章做了什么”。
更推荐讲成下面这条线：
1. 多元长程时间序列预测为什么难。
2. 本文先构建统一预测骨架。
3. 在统一骨架上提出两条互补的频域增强路线。
4. 系统实验说明两条路线都有效，而且各有侧重。

==================================================
一、逐页怎么做
==================================================

第 1 页 封面
页标题
融合大语言模型语义先验与时频建模的多元长程时间序列预测方法研究

页面文字
答辩人：XXX
指导教师：XXX
学院：XXX
日期：XXXX 年 XX 月 XX 日

建议配图
不需要复杂配图，放学校 logo 或简洁底色即可。

建议讲稿
各位老师好，我的硕士学位论文题目是《融合大语言模型语义先验与时频建模的多元长程时间序列预测方法研究》。下面我将从研究背景、方法设计、实验结果和结论四个方面进行汇报。

--------------------------------------------------

第 2 页 研究背景与任务定义
页标题
多元长程时间序列预测需要同时刻画趋势、波动与跨变量关联

页面文字
- 任务：利用历史多变量序列预测未来较长时间范围的变化
- 难点：长期趋势、局部突变、多尺度波动并存
- 挑战：跨变量关联复杂，误差容易随预测步长累积

建议配图
放一个任务示意图即可：输入历史窗口，输出未来预测窗口。
如果来不及重画，自己用 PPT 画一个简洁框图就够了。

建议讲稿
本文研究的是多元长程时间序列预测问题。与短期预测相比，长程预测不仅要把握整体趋势，还要兼顾局部波动和跨变量关联，因此模型既要有足够的表达能力，也要尽量控制长预测步长下的误差累积。

--------------------------------------------------

第 3 页 研究问题与论文思路
页标题
本文的核心思路是在统一骨架下融合语义先验与频域增强

页面文字
- 仅依赖时域建模时，难以同时兼顾局部突变与长期周期
- 传统时序模型受限于参数规模与数据规模，LLM 可提供更稳健表征，但与数值序列存在模态差异
- 目标：在统一框架中稳定融合语义信息与频域信息

建议配图
这一页不必放复杂模型图。
用“三个问题框 + 一个目标框”的示意结构最清楚。

建议讲稿
围绕这一任务，本文主要关注两个问题。第一，仅依赖时域建模时，模型难以同时刻画局部突变、多尺度波动和长期周期。第二，传统统计方法和深度学习方法往往受限于可学习参数规模以及时间序列训练数据规模，性能和鲁棒性仍有提升空间；而大语言模型在海量语言数据上预训练，能够提供更稳健的表征和变量级语义先验，但直接融入数值预测流程时又会面临模态差异和对齐困难。基于此，本文希望在统一框架下，把语义先验和频域增强有效结合起来。

--------------------------------------------------

第 4 页 统一预测骨架
页标题
统一预测骨架为两类频域增强方法提供共享接口

页面文字
- 时域分支：提取基础动态表示
- 频域分支：补充周期与谱结构信息
- 语义分支：利用冻结 Qwen3-0.6B 提供变量级先验
- 融合方式：步长感知门控 + 跨模态注意力对齐

建议配图
直接使用：
docs/NEU-Thesis-main/NEU-Thesis-main/Img/chap3_framework.png

建议讲稿
为解决前述问题，本文首先构建了统一预测骨架。这个骨架把时域建模、频域表示和冻结大语言模型生成的变量级语义嵌入纳入同一框架，并通过步长感知门控融合和跨模态注意力对齐实现协同建模。它的意义不只是搭了一个基础模型，更重要的是为后续两类频域增强方法提供了统一接口和公平比较基础。

--------------------------------------------------

第 5 页 方法一：小波包局部多尺度增强
页标题
小波包增强方法更擅长刻画局部异常与多尺度波动

页面文字
- 通过可学习小波包分解，将序列拆成多尺度子带
- 通过子带级编码、频带交互与注意力池化提取局部结构
- 重点增强对尖峰、突变和复杂局部波动的建模能力

建议配图
优先使用：
docs/NEU-Thesis-main/NEU-Thesis-main/Img/chap4_wavelet_packet_overview_v2.png

如果版面允许，可在右下角补一张：
docs/NEU-Thesis-main/NEU-Thesis-main/Img/chap4_wavelet_packet_decomposition_python.png

建议讲稿
在第四章中，本文提出了融合小波包分解的局部多尺度频域增强方法。它的核心思想是先把原始序列按不同频带进行递归拆分，再分别对各子带进行编码和聚合。这样做的好处是，模型可以把长期趋势、低频起伏和高频扰动区分开来建模，因此更适合处理尖峰、突变和复杂波动较明显的场景。

--------------------------------------------------

第 6 页 方法二：FreTS 全局频域增强
页标题
FreTS 增强方法更擅长建模全局周期结构与长期趋势

页面文字
- 先通过 RFFT 将序列映射到复数频谱空间
- 再通过复数变换、SoftShrink 稀疏化和 iRFFT 完成重构
- 重点强化对主导频率、全局周期和长期趋势的建模

建议配图
这一页放总图：
docs/NEU-Thesis-main/NEU-Thesis-main/Img/chap5_overview_user.png

如果想拆成两页或版面更宽，可补一张细节图：
docs/NEU-Thesis-main/NEU-Thesis-main/Img/chap5_frets_frequency_process_v1.png

建议讲稿
在第五章中，本文提出了融合频域前馈网络的全局频域增强方法。与第四章不同，这一方法不再强调局部子带递归分解，而是直接进入全局频谱空间进行可学习变换和稀疏化处理，再通过步长感知门控回到统一预测流程。相比第四章，它更强调全局周期结构、整体曲线形态和长期趋势保持。

--------------------------------------------------

第 7 页 实验设置
页标题
实验在多场景公开数据集上验证方法的有效性

页面文字
- 数据集：ETT、Weather、ILI、Exchange 等
- 指标：MSE、MAE
- 对比模型：T3Time、TimeCMA、TimeLLM、UniTime、PatchTST、DLinear、iTransformer 等

建议配图
这一页不建议贴大表。
做成“三列卡片式概览”最清楚：数据集、指标、基线模型。

可直接放入 PPT 的表
| 数据集 | 评价指标 | 对比模型 |
| --- | --- | --- |
| ETT、Weather、ILI、Exchange 等；覆盖工业负荷、气象、公共卫生、金融等典型场景 | MSE、MAE；分别衡量整体误差和平均绝对偏差 | T3Time、TimeCMA、TimeLLM、UniTime、PatchTST、DLinear、iTransformer 等；覆盖统一建模、大模型、Transformer 和线性方法 |

建议讲稿
为了保证实验结论具有代表性，本文在工业负荷、气象、公共卫生和金融等不同场景的数据集上进行了系统实验，并统一使用 MSE 和 MAE 作为评价指标。同时，选取了大模型时间序列方法、Transformer 类方法和线性方法作为对比对象，以保证比较尽可能充分和公平。

--------------------------------------------------

第 8 页 主结果
页标题
两类频域增强路线在统一框架下都表现出稳定收益

页面文字
- 两种方法在多数据集、多预测长度设置下都具有较强竞争力
- 第四章方法更适合复杂波动和局部大误差抑制
- 第五章方法更适合全局周期组织与长期趋势保持

建议配图
不要直接贴论文完整大表。
建议你自己做一个“结论型精简表”，只保留 3 行结论：
1. 统一骨架有效
2. 小波包增强有效
3. FreTS 增强有效

建议讲稿
从主结果来看，在统一训练流程下，两类频域增强方法都在多数据集和多预测长度设置中表现出较强竞争力。更重要的是，二者并不是重复做同一件事。第四章更偏向局部多尺度波动建模，第五章更偏向全局周期结构和长期趋势组织。这说明在统一骨架下引入频域增强是有效的，而且两条技术路线具有互补性。

--------------------------------------------------

第 9 页 可视化与消融分析
页标题
可视化与消融结果说明性能提升并非偶然

页面文字
- 预测曲线更贴近真实峰谷和整体趋势
- 去除 WPD 或 FreTS 后，性能都会明显退化
- 说明关键增益来自频域模块与融合机制的协同作用

建议配图
优先选两张最有说服力的图：
docs/NEU-Thesis-main/NEU-Thesis-main/Img/selected_pred_figs/chap5_etth1_pred192_main.png
docs/NEU-Thesis-main/NEU-Thesis-main/Img/selected_pred_figs/chap5_weather_pred336_ablation_wo_frets.png

如果你想突出第四章，也可以把第二张换成：
docs/NEU-Thesis-main/NEU-Thesis-main/Img/selected_pred_figs/chap4_result_etth1_hufl.png

建议讲稿
除主结果外，本文还通过可视化和消融实验进一步分析方法有效性。从可视化结果可以看到，增强模型在峰谷位置、转折形态和长期趋势上都更贴近真实序列。从消融结果来看，无论去除小波包分支还是去除 FreTS 分支，性能都会出现明显退化，这说明模型性能提升并不是偶然现象，而是来源于频域增强模块和融合机制的共同作用。

可选补充数据
如果老师追问，你可以举两个最直接的例子：
1. 第四章中，去除 WPD 后，ETTh1 平均结果由 0.427/0.430 退化到 0.504/0.490。
2. 第五章中，去除 FreTS 后，Weather 平均结果由 0.235/0.261 退化到 0.257/0.284。

--------------------------------------------------

第 10 页 结论与创新点
页标题
本文验证了语义先验与频域增强协同建模的可行性

页面文字
- 构建统一预测骨架，融合时域、频域与冻结语义先验
- 提出小波包局部多尺度增强方法
- 提出 FreTS 全局频域增强方法，并完成系统实验验证

建议配图
这一页尽量不放复杂图，直接做成“3 个创新点 + 1 句总结”的版式。

建议讲稿
本文的主要工作可以概括为三点。第一，构建了融合时域、频域和冻结语义先验的统一预测骨架。第二，在该骨架上提出了基于小波包分解的局部多尺度增强方法。第三，在同一骨架上提出了基于 FreTS 的全局频域增强方法，并通过系统实验验证了两类方法的有效性。总体来看，本文验证了语义先验与频域增强协同建模在多元长程时间序列预测中的可行性。

--------------------------------------------------

第 11 页 致谢与答辩结束
页标题
感谢各位老师聆听，恳请批评指正

页面文字
谢谢各位老师

建议配图
不需要额外配图，保持干净即可。

建议讲稿
我的汇报完毕，感谢各位老师聆听，恳请各位老师批评指正。

==================================================
二、PPT 制作原则
==================================================

1. 一页只讲一个核心信息，不要一页塞两个结论。
2. 标题尽量写成结论句，不要只写“方法介绍”“实验结果”。
3. 每页正文尽量控制在 3 行左右，老师不会看大段文字。
4. 方法页优先放结构图，结果页优先放曲线图，少放整张大表。
5. 全文颜色建议只保留三种：蓝色表示统一骨架，橙色表示第四章，绿色表示第五章。
6. 动画只用“逐条出现”，不要使用花哨切换。

==================================================
三、答辩时最该反复强调的三句话
==================================================

1. 本文不是把多个模块简单堆叠，而是先构建了统一预测骨架。
2. 第四章和第五章是在统一骨架上的两条互补频域增强路线。
3. 实验结果说明，语义先验与频域增强的协同建模是有效的。

==================================================
四、老师高频提问的简短回答
==================================================

问题 1：为什么要引入大语言模型语义先验？
回答
因为传统统计方法和深度学习方法往往受限于可学习参数规模以及时间序列训练数据规模，性能和鲁棒性仍有瓶颈；而多元时间序列中的变量又并不是孤立的，变量名称和任务背景本身包含一定语义信息。大语言模型在海量语言数据上预训练，能够提供更稳健的表征和变量级语义先验。本文并不是让大语言模型直接生成预测值，而是把它作为冻结的外部语义编码器，为变量关系建模提供补充先验，并通过对齐与融合机制降低模态差异带来的干扰。

问题 2：为什么采用冻结而不是全量微调？
回答
一方面，全量微调计算开销更大，也更容易在时间序列数据规模有限的情况下出现过拟合。另一方面，本文的目标是稳定利用语义先验，因此采用冻结主体加轻量适配的方式，在效率和效果之间更平衡。

问题 3：第四章和第五章的区别是什么？
回答
第四章重点解决局部多尺度波动问题，更适合刻画尖峰、突变和复杂局部异常；第五章重点解决全局周期和长期趋势问题，更适合建模整体频谱结构和长期趋势保持。

问题 4：为什么需要两种频域增强方法？
回答
因为时间序列往往同时包含局部异常和全局周期，仅用一种频域视角很难兼顾两类结构。本文在统一骨架下分别验证了局部多尺度增强和全局频域增强的有效性，也说明二者具有互补性。

问题 5：如何说明性能提升不是偶然的？
回答
本文不仅给出了主结果，还补充了控制实验、可视化和消融实验。无论是替换语义编码器、去除门控机制，还是移除 WPD 或 FreTS 模块，都会导致性能下降，因此可以说明增益来自关键结构设计，而不是偶然波动。

问题 6：本文还有哪些不足？
回答
目前实验主要基于公开数据集，评价指标以 MSE 和 MAE 为主，对真实部署成本、推理效率和更多业务指标的分析还不够充分。因此后续还需要在真实场景泛化、语义先验强化和模型高效部署方面继续研究。

==================================================
五、最后提醒
==================================================

你答辩时最稳妥的讲法，不是强调“我做了很多模块”，而是强调“我围绕一个清晰问题，搭建了一个统一框架，并在这个框架上提出了两条有针对性的改进路线，最后用实验说明它们是有效的”。

只要你把这条主线讲清楚，老师通常会觉得你的论文逻辑更完整，结构也更合理。
